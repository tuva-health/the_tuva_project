version: 2

models:

  - name: normalize__unmapped_condition
    description: >
      unmapped condition codes
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_core
        {% else %}normalize{%- endif -%}
      alias: condition_unmapped
      tags: normalized
      materialized: table

  - name: normalize__unmapped_procedure
    description: >
      unmapped procedure codes
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_core
        {% else %}normalize{%- endif -%}
      alias: procedure_unmapped
      tags: normalized
      materialized: table

  - name: normalize__unmapped_lab_result
    description: >
      unmapped procedure codes
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_core
        {% else %}normalize{%- endif -%}
      alias: lab_result_unmapped
      tags: normalized
      materialized: table

  - name: normalize__unmapped_medication
    description: >
      unmapped procedure codes
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_core
        {% else %}normalize{%- endif -%}
      alias: medication_unmapped
      tags: normalized
      materialized: table

  - name: normalize__unmapped_observation
    description: >
      unmapped procedure codes
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_core
        {% else %}normalize{%- endif -%}
      alias: observation_unmapped
      tags: normalized
      materialized: table